const
    expect           = require('expect'),
    {describe, test} = require('mocha'),
    objects          = require('../src/objects.js');

describe('fua.core.objects', function () {

    test('develop', function () {
        console.log(objects);
    });

});
